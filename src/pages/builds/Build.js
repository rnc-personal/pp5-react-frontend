import React from 'react'

const Build = (props) => {

    const {
        id,
        creator,
        profile_id,
        profile_image,
        build_name,
        build_cpu,
        build_mobo,
        build_ram,
        build_disk,
        build_gpu,
        build_case,
        build_monitor,
        build_,
        content,
        main_image,
        updated_at,
        postPage,
        setPosts,
      } = props;

  return (
    <div>Build Detail</div>
  )
}

export default Build